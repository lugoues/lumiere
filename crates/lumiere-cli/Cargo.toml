[package]
name = "lumiere-cli"
description = "CLI client for the Lumière daemon, plus the hardware probe tool"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "lumiere"
path = "src/main.rs"

[dependencies]
lumiere-proto.workspace = true
